import React from "react";
import { Element } from "react-scroll";

const Portfolio = () => {
  return (
    <section className="bg-primary section h-[100vh]">
      <div className="element flex justify-center w-full">PortFolio</div>
    </section>
  );
};

export default Portfolio;
